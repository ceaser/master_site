
== Getting started
1. Use the version of ruby identified in the ruby_version file in the root
directory of the project.

2. Install gems by running the `bundle install`

3. Initialize the project by running `rake init`

4. Add hosts names to your hosts config file

5. Start the rails server and visit http://localhost:3000 for the master site
http://www.oceanic.com:3000 or http://www.acme.com:3000 for customer sites

6. If you add additional client you must run
`rake db:customer:create db:customer:migrate` and add them to your host file
